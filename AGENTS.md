# Life Plan Simulator

FIRE（経済的自立・早期リタイア）可能性をシミュレーションする Web アプリ。

## 概要

- 現在の資産・収入・支出・ライフイベントなどを入力し、資産推移と枯渇時期をシミュレートする
- ブラウザのみで動作する静的 Web アプリ（GitHub Pages でホスト）
- バックエンド・サーバーサイド処理は持たない

## ゴール

1. 入力した前提条件から、資産がいつ頃尽きるか／FIRE 達成可能かを可視化する
2. 入力項目をすべてカスタマイズ可能にし、シミュレーション精度を上げられるようにする
3. 設定を JSON でエクスポート／インポートし、前提の保存・復元・共有ができるようにする
4. パラメータを変えた比較（例: 退職年齢、生活費）ができるようにする

## 機能要件

### 入力

シミュレーションに必要な値はすべてユーザーが入力・編集できること。例:

| カテゴリ | 項目例 |
|----------|--------|
| 基本 | 現在の年齢 |
| 資産 | 現在の金融資産 |
| 収支 | 毎月の生活費、就労中の収入、退職後の収入（サイドFIRE等） |
| 家族 | 家族構成（配偶者・子供の有無、年齢など） |
| ライフイベント | 子供の誕生、大学入学、住宅購入、退職など（時期と金額影響） |

- デフォルト値を用意し、未入力でも試せるようにする
- バリデーション（負の値、年齢の整合性など）を行う

### シミュレーション

- 年次（または月次）で資産残高を投影する
- 就労期間中の積立・収入、退職後の取り崩しを反映する
- ライフイベントによる一時的・継続的な収支変化を反映する
- 投資リターン・インフレ率などは設定可能な前提として扱う
- 結果として少なくとも次を示す:
  - 資産推移（グラフおよび／または表）
  - 資産枯渇の有無と時期
  - FIRE 達成の可否・時期（定義は実装時に明文化）

### シナリオ比較

次のような「前提を変えた場合」を並べて比較できること:

- 退職（FIRE）年齢の違い（例: 40 歳 / 45 歳）
- 生活費の違い
- その他、主要パラメータの what-if

比較 UI の具体形（タブ、並べ表示、差分ハイライトなど）は実装時に決める。

### 設定の永続化（JSON）

- 現在の入力・前提を JSON ファイルとしてエクスポートできる
- エクスポートした JSON をインポートして設定を復元できる
- スキーマはバージョン管理し、将来の項目追加に耐える形にする

### 非機能

- 静的ホスト（GitHub Pages）前提
- デプロイは GitHub Actions で行う。コード変更（例: メインブランチへの push / merge）をトリガーにビルドし、GitHub Pages へ自動公開する
- 個人情報・資産データはサーバーに送らない（すべてクライアント側）
- レスポンシブ（PC 主想定、タブレットでも利用可能が望ましい）
- 日本語 UI

## 技術選定

| 項目 | 選定 |
|------|------|
| 言語 | TypeScript |
| UI | React 19 |
| ビルド | Vite 8 |
| テスト | Vitest |
| グラフ | SVG 自前実装（依存追加なし） |
| ホスト | GitHub Pages（`base: './'`） |
| CI/CD | GitHub Actions（test → build → Pages） |

## ディレクトリ構成

```
src/
  engine/          # 計算ロジック（UI 非依存・単体テスト対象）
    types.ts
    defaults.ts
    simulate.ts
    validate.ts
    serialize.ts
  components/      # 画面コンポーネント
  lib/             # 表示用ユーティリティ
  App.tsx
  main.tsx
  index.css
.github/workflows/deploy.yml
```

## 開発コマンド

```bash
npm install
npm run dev      # 開発サーバ
npm test         # 単体テスト
npm run build    # 本番ビルド → dist/
npm run preview  # ビルド結果のプレビュー
npm run lint     # oxlint
```

## シミュレーション前提（実装）

| 項目 | 内容 |
|------|------|
| 計算単位 | **月次**で資産を更新し、**年次**スナップショットを表示（終了年齢既定 **90**） |
| 投資リターン | 固定年率（既定 **5%**）。月次複利 ` (1+r)^(1/12)-1 ` |
| インフレ | 生活費・収入（就労中／退職後）・イベント金額に年率（既定 **2%**）を複利適用（実質一定） |
| 貯蓄モデル | 月次 CF = 収入 − 生活費 + イベント。就労中は `monthlyIncome`、退職後は `monthlyPostRetireIncome`（サイドFIRE等、既定 0） |
| 運用益課税 | 正の運用益に税率（既定 **20.315%**・上場株式等の申告分離課税相当）を月次で控除。0% で非課税 |
| 税金・社保・年金 | 給与所得税・社会保険・公的年金は **未考慮** |
| FIRE 定義 | 期末資産 ≧ その年の年間生活費 ÷ 安全取出率（既定 **4%**）となった最初の年齢 |
| 枯渇 | 期末資産が 0 円になった最初の年齢 |
| 大学費用 | 子供ごとに 18〜21 歳の 4 年、総額 400 万円（インフレ調整）を自動計上（任意） |

### JSON スキーマ

```json
{
  "schemaVersion": 1,
  "exportedAt": "ISO-8601",
  "plan": { "...PlanInput" }
}
```

`schemaVersion` で後方互換を管理する。

## やらないこと（初期スコープ外）

- ユーザーアカウント、クラウド同期
- 実口座連携、証券 API
- 税務・法律の正式な助言機能
- ネイティブアプリ

## 受け入れの目安

- [x] 主要入力を変更するとシミュレーション結果が即座に更新される
- [x] 資産推移と枯渇／FIRE 関連の結果が分かる
- [x] 退職年齢や生活費を変えた比較ができる
- [x] 設定の JSON エクスポート／インポートができる
- [ ] GitHub Pages 上で動作する（main へ push 後、リポジトリの Pages 設定が必要）
- [x] コード変更時に GitHub Actions で自動デプロイされる（workflow 追加済み）
- [x] データがサーバーに送信されない
